export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-semibold text-text-primary">My App</h1>
      <p className="text-text-secondary">
        Scaffolded with react-web-plugin — replace this page with your landing or redirect to the
        dashboard.
      </p>
    </main>
  );
}
