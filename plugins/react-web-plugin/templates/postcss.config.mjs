// Tailwind CSS v4 — the PostCSS plugin is all that's needed; tokens live in
// app/globals.css via @theme (no tailwind.config.js).
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
